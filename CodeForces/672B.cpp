#include <iostream>

using namespace std;

int main() {
    int n;
    string s;
    cin >> n >> s;
    if (n.size() > 26) cout << -1 << endl;
    else {
        int c = 0;
        for (int 
    }
    return;
}
