/**
 * The Daily Byte
 *
 * Compression
 * Given a character array, compress it in place and return the new length of
 * the array. Note: YOu should compress the array if its compressed form will
 * be at least as short as the length of its original form.
 */

#include <vector>

int main()
{
    return 0;
}
