#include <stdio.h>
#include <vector>
#include <string.h>
#include <map>
#define FOR(x, size) for (int x = 0; x < size; x++)
#define gc getchar_unlocked

using namespace std;

int main() {
    long l, b;
    while (scanf("%l", &l) != EOF) {

    }
    return 0;
}
