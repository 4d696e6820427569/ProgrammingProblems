/**
 * SIG 2
 * Implmeent a simplified version of a crypto broke rplatform. Initially, there
 * is an array of users, with each user depositing only cash - deposits[i] is
 * the amount of cash the (i + 1) th user has deposited.
 *
 * Assume that for now the platform contains just a single stock and all
 * operations are done with this stock only. All operations are supposed to be
 * in the same currency, and currency exchange is not supported for now.
 *
 * The platform shoud lsupport the following 3t ypes of operations
