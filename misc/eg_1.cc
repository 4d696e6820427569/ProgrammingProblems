/**
 * EG problem 1
 *
 * Create unique device names to be used in a resident IOT system. If a device
 * name already exists in the system, an integer number is added at the end of
 * the name to make it unique. The integer added starts with 1 and is
 * incremented by 1 for each requests, process all requests and return an array
 * of corresponding unique device names.
 *
 * Example:
 * n = 6
 * devicenames = {'switch', 'tv', 'switch', 'tv', 'switch', 'tv'}
 *
 * return uniqueDevicenames = {'switch', 'tv', 'switch1', 'tv1', 'switch2', 'tv2'}
 *
 * Constraints:
 * 1 <= n <= 10^4
 * 1 <= length of devicenames[i] <= 20.
 */

#include <vector>
#include <string>

using std::vector;
using std::string;

vector<string> deviceNamesSystem(vector<string> devicenames)
{
    vector<string> result;
    return result;
}


int main()
{
    return 0;
}
