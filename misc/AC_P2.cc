/**
 * AC C++ Dev P2
 *
 * Given a max budget b, a number of cities n, and the array of money to spend
 * for each city, return the maximum expediture cost.
 */

unsigned MaximumPriceNotOverBudget(unsigned b, unsigned n, unsigned price[])
{
    unsigned res = 0;
    return res;
}
